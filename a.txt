
IPv6 is now coming about because we are running very low on IPv4 addresses. IPv6 has powerful enhancements that better suit today’s network demands. Improvements include:Increased address space - IPv6 is based on 128-bit hierarchical addressing as IPv4 is 32-bit.Improved packet handling - IPv6 has been simplified with fewer fields.Eliminates the need for NAT – Since there are so many IPv6 addresses, there is no need for NAT and its application problems.Discuss the purpose of the fields in an IPv6 header (refer to https://static-course-assets.s3.amazonaws.com/ITN6/en/index.html#6.1.4.4 ):Version – Contains a 4-bit binary value set to 0110 that says this as an IPv6 packet.Traffic Class – This 8-bit field is the equivalent of IPv4 DS field.Flow label – this 20-bit field suggests that all packets with the same flow label receive the same type of handling on routers.Payload Length - This 16-bit field indicates the length of the data portion or payload of the IPv6 packet.Hop Limit – This 8-bit field replaces the IPv4 TTL field. Ig timed out, it sends a message back to the source indicating it failed.Source IPv6 address – This 128-bit field IDs the IPv6 address of the source.Destination IPv6 address – This 128-bit field IDs the IPv6 address of the destination.A default gateway…Routes traffic to other networksHas a local IP address in the same address range as other hosts on the network.Can take data in and forward data out
Az IPv6 most jön létre, mert nagyon kevés az IPv4-címünk. Az IPv6 erőteljes fejlesztésekkel rendelkezik, amelyek jobban megfelelnek a mai hálózati igényeknek. A fejlesztések a következők: Megnövelt címterület – Az IPv6 128 bites hierarchikus címzésen alapul, mivel az IPv4 32 bites.Továbbfejlesztett csomagkezelés – Az IPv6 egyszerűsödött, kevesebb mezővel. Megszünteti a NAT szükségességét – Mivel nagyon sok az IPv6 címeket, nincs szükség NAT-ra és annak alkalmazási problémáira. Beszélje meg a mezők célját egy IPv6-fejlécben (lásd: https://static-course-assets.s3.amazonaws.com/ITN6/en/index.html# 6.1.4.4 ):Version – 0110-re beállított 4 bites bináris értéket tartalmaz, amely ezt IPv6-csomagként mondja.Traffic Class – Ez a 8 bites mező megegyezik az IPv4 DS mezővel.Flow címke – ez a 20- bit mező azt sugallja, hogy az azonos folyamatcímkével rendelkező csomagok azonos típusú kezelést kapnak az útválasztókon. Payload Length - Ez a 16 bites mező az IPv6-csomag adatrészének vagy hasznos terhelésének hosszát jelzi. Hop Limit - Ez a 8- bit mező váltja fel az IPv4 TTL mezőt. Az Ig időtúllépése miatt üzenetet küld a forrásnak, jelezve, hogy sikertelen.Forrás IPv6-cím – Ez a 128 bites mező a forrás IPv6-címét azonosítja.Cél IPv6-címe – Ez a 128 bites mező azonosítja a forrás IPv6-címét. rendeltetési hely.Alapértelmezett átjáró… A forgalmat más hálózatok felé irányítja
